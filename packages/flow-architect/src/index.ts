/**
 * Flow Architect package.
 *
 * The dynamic browser-based discovery lives in agent-core/FlowArchitectAgent.
 * This package extends it with static analysis (filesystem, route configs).
 */

export * from './static-analyzer.js';
